# ------------------------------------------------------------------
# Optional environment variables
# ------------------------------------------------------------------

# Contact form endpoint.
# Leave this UNSET to use the zero-config mailto fallback (opens the
# visitor's email client). To collect submissions automatically, create
# a form endpoint (e.g. Formspree, Resend, or your own /api route) and
# paste the URL here.
NEXT_PUBLIC_FORM_ENDPOINT=
